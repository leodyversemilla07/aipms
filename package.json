{
  "name": "aipms",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "check": "biome check .",
    "lint": "biome lint .",
    "format": "biome check --write .",
    "typecheck": "turbo typecheck",
    "db:generate": "turbo run db:generate",
    "db:deploy": "turbo run db:deploy",
    "db:migrate": "turbo run db:migrate",
    "db:push": "turbo run db:push",
    "db:reset": "turbo run db:reset",
    "db:seed": "turbo run db:seed",
    "db:studio": "turbo run db:studio",
    "auth:generate": "turbo run auth:generate",
    "capacity:smoke": "node scripts/capacity-smoke.mjs",
    "staging:validate": "node scripts/staging-validate.mjs",
    "staging:browser-workload": "node scripts/staging-browser-workload.mjs",
    "deployment:preflight": "node scripts/deployment-preflight.mjs",
    "release:gate": "node scripts/release-gate.mjs",
    "release:gate:test": "node scripts/test-release-gate.mjs",
    "operations:check": "node scripts/operations-check.mjs",
    "operations:check:test": "node scripts/test-operations-check.mjs"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.7",
    "@workspace/typescript-config": "workspace:*",
    "turbo": "^2.9.18",
    "typescript": "^5"
  },
  "packageManager": "pnpm@11.23.0",
  "engines": {
    "node": "24.x"
  }
}
