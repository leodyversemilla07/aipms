import { defineListTool } from "../lib/list-tool"

export default defineListTool(
  "vendor",
  "List a page of vendors. Search with q and inspect returned statuses; use page and pageSize for pagination."
)
